query Laboratories($id: ID!) {
    getMatchableLaboratories(id: $id) {
        id
        university {
            name
        }
        name
        comment
        majors {
            name
        }
        status
    }
}