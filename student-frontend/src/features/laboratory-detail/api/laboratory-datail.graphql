query  Laboratory($id: ID!) {
    laboratory(id: $id) {
        id
        university {
            name
        }
        name
        professor
        numStudents
        comment
        status
        imageUrl
        laboratoryUrl
        majors {
            name
        }
    }
}